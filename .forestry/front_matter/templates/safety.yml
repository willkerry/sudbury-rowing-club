---
label: Safety
hide_body: true
fields:
- type: field_group_list
  name: safety
  label: Safety Notices
  fields:
  - type: text
    name: name
    label: Title
    config:
      required: true
  - type: textarea
    name: description
    label: Description
    config:
      wysiwyg: true
      schema:
        format: html-blocks
  - type: datetime
    name: date
    label: Date updated
    config:
      date_format: DD MMMM YYYY
  - name: file
    type: file
    config:
      maxSize: 64
    label: Document upload
pages:
- data/safety.json
